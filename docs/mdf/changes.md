# MDF Changes Log

> Auto-generated by mdf-record. Do not edit manually.
> Run `/mdf-summarize` to categorize and clear this log.

<!-- MDF_HEADER_END -->

---
## 2026-03-29T10:00:00Z

**任务**: 修复 Windows Stop Hook 兼容性问题，改为 CLAUDE.md 指令方案

### 改动原因
Windows 上 `claude -p /mdf-record` 无论通过何种方式调用（直接调用、node 包装、git-bash 包装）都报 git-bash 依赖错误，根本原因是 claude CLI 内部强制检查运行环境，不是调用层面能绕开的。

### 决策
- 放弃 Stop Hook 方案（暂时），改用 CLAUDE.md 指令方案：在 CLAUDE.md 加 `## Auto-record` 规则，要求 Claude 每次任务结束前执行 `/mdf-record`
- `settings.json` 清空 Hook 配置
- `.claude/hooks/mdf-record.js` 保留（探索过程产物，后续可继续研究）
- Windows 用户若要用 Hook 方案，需设置系统环境变量 `CLAUDE_CODE_GIT_BASH_PATH=D:\Program Files\Git\bin\bash.exe`

### 结果
- CLAUDE.md 新增 Auto-record 节，三平台通用
- settings.json Hook 已清除
- README.md 中 Hook command 已同步更新（node 包装版本，供参考）
